
import { ElementFinder } from 'protractor';

import { ArtistComponent } from './artist.component';

describe('Artist: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new ArtistComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
