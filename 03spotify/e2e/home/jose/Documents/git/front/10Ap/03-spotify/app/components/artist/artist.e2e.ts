import { Artist } from './artist.protractor';

describe('Artist E2E', () => {

    let artist: Artist = new Artist;

    beforeAll(() => {
    });

    it('should check that ...', () => {
        // expect(XXX).toBeTruthy();
    });
});
